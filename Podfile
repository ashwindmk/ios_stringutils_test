# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'IOSStringUtilsTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IOSStringUtilsTest
  #pod 'IOSStringUtils', :path => '~/iOS/IOSStringUtils'
  pod 'IOSStringUtils', '-> 0.1.1'

  target 'IOSStringUtilsTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'IOSStringUtilsTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
